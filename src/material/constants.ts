// tslint:disable no-magic-numbers

import { as, Duration, Scalar } from '@musical-patterns/utilities'

const OMNIZONK_FOREVER: Scalar<Duration> = as.Scalar<Duration>(9999)

export {
    OMNIZONK_FOREVER,
}
