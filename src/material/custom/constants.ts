// tslint:disable no-magic-numbers

import { as, Gain, Scalar } from '@musical-patterns/utilities'

const OMNIZONK_GAIN_SCALAR_SCALAR: Scalar<Scalar<Gain>> = as.Scalar<Scalar<Gain>>(0.01)

export {
    OMNIZONK_GAIN_SCALAR_SCALAR,
}
