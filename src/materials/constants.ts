// tslint:disable:no-magic-numbers

import { Scalar, to } from '@musical-patterns/utilities'

const OMNIZONK_FOREVER: Scalar = to.Scalar(99999999)
const OMNIZONK_GAIN: Scalar = to.Scalar(0.0001)

export {
    OMNIZONK_FOREVER,
    OMNIZONK_GAIN,
}
